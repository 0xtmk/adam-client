import { Theme } from "@/types/core.type"

export const appConfig = {
  // Config version
  version: "0.0",
  // Config default theme
  theme: "dark" as Theme,
  solanaSignMessage: "ADAM SIGN",

} as const
